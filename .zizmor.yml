rules:
  # Weekly Dependabot updates are acceptable for a small personal repo.
  dependabot-cooldown:
    ignore:
      - dependabot.yml
  # release-please needs a PAT to trigger CI on its PRs. Personal repo,
  # no environment gates needed.
  secrets-outside-env:
    ignore:
      - release-please.yml
