{
  "tag": "latest",
  "build_args": "DAGU_VERSION=2.3.7",
  "test_commands": [
    "docker run --rm $IMAGE_REF sh -c \"dagu version && restic version && rclone version | head -1 && docker --version && ssh -V 2>&1 && curl --version | head -1\""
  ]
}
