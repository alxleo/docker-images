ignored:
  - DL3008  # pin apt versions — impractical, old versions removed from repos on base image update
  - DL3016  # pin npm versions — version comes from ARG, pinned in mcp-images.json
  - DL3018  # pin apk versions — same as DL3008
  - DL3059  # consecutive RUN — intentional for Docker cache layers

trustedRegistries:
  - docker.io
  - ghcr.io
