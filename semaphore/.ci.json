{
  "tag": "v2.17.33",
  "test_commands": [
    "docker run --rm $IMAGE_REF sh -c \"sops --version && age --version && dig -v 2>&1 && curl --version | head -1 && jq --version && rsync --version | head -1 && just --version && uv --version && tailscale version && docker --version\""
  ]
}
