{
  "tag": "v2.9.1",
  "build_args": "MCP_AUTH_PROXY_VERSION=v2.9.1",
  "test_setup": "pip install -q uv",
  "test_commands": [
    "uv run --with pytest --with requests -- pytest test/test_custom_images.py -m mcp_auth_proxy -v"
  ]
}
