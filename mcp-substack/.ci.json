{
  "tag": "1.0.0",
  "build_args": "MCP_PROXY_VERSION=6.5.4",
  "test_commands": [
    "docker run --rm --entrypoint sh $IMAGE_REF -c \"python3 --version | grep -F 'Python 3.14.' && node --version | grep -E '^v26\\\\.'\"",
    "docker run --rm -d --name mcp-substack-test -e SUBSTACK_USERNAME=test -p 18080:8080 $IMAGE_REF && bash test/test-mcp-smoke.sh mcp-substack-test 18080 && docker stop mcp-substack-test || { docker rm -f mcp-substack-test 2>/dev/null || true; false; }"
  ],
  "watch_paths": [
    "test/test-mcp-smoke.sh"
  ]
}
