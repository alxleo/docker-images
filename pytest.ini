[pytest]
markers =
    caddy_cloudflare: Tests for the caddy-cloudflare custom image
    cadvisor: Tests for the cAdvisor custom image
    git_mcp_server: Tests for the git-mcp-server custom image
    mcp_auth_proxy: Tests for the mcp-auth-proxy custom image
    mcp_metadata: Tests for MCP image metadata (EXPOSE, labels)
